// GENERATED by scripts/generate-theme-boot.mjs — do not edit.
// Runs before first paint so a stored palette does not flash the static fallback.
(function () {
  var PALETTES = {
    "remarque-dark": {
      "bg": "oklch(0.16 0.01 80)",
      "fg": "oklch(0.9 0.005 80)",
      "meta": "#100d09"
    },
    "remarque-light": {
      "bg": "oklch(0.975 0.005 80)",
      "fg": "oklch(0.18 0.01 80)",
      "meta": "#f8f6f3"
    },
    "green-phosphor-crt": {
      "bg": "oklch(0.162 0.011 145.1)",
      "fg": "oklch(0.872 0.279 142.7)",
      "meta": "#0b0f0b"
    },
    "keys-ocean-sunset-hc": {
      "bg": "oklch(0.14 0.017 263.5)",
      "fg": "oklch(0.966 0.009 258.3)",
      "meta": "#060910"
    },
    "progros": {
      "bg": "oklch(0.55 0.12 185)",
      "fg": "oklch(0.15 0.01 185)",
      "meta": null
    }
  };
  var KEY = "progquest_theme_v1";
  var stored = null;
  try {
    stored = window.localStorage.getItem(KEY);
  } catch {
    // Storage denied. Fall through to the system preference exactly as the app does; a theme
    // is a preference, not a reason to fail loading.
    stored = null;
  }
  var prefersDark = false;
  try {
    prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
  } catch {
    prefersDark = false;
  }
  var theme = Object.prototype.hasOwnProperty.call(PALETTES, stored)
    ? stored
    : (prefersDark ? "remarque-dark" : "remarque-light");
  var palette = PALETTES[theme];
  if (!palette) return;

  var root = document.documentElement;
  root.dataset.theme = theme;
  if (theme !== 'progros') root.dataset.terminalTheme = theme;
  root.style.setProperty('--terminal-background', palette.bg);
  root.style.setProperty('--terminal-foreground', palette.fg);
  if (palette.meta) {
    var meta = document.querySelector('meta[name="theme-color"]');
    if (meta) meta.setAttribute('content', palette.meta);
  }
})();
